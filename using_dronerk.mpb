feature (dronerk) {
  macros += _GAMS_DRONERK_

  includes += $(DRK_ROOT)/include
  libpaths += $(DRK_ROOT)/lib
  libs += drk

  Header_Files {
    Platforms {
      src/gams/platforms/dronerk
    }
  }

  Source_Files {
    Platforms {
      src/gams/platforms/dronerk
    }
  }
}
