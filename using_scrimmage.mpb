feature (scrimmage) {
  requires += scrimmage

  macros += _SCRIMMAGE_

  includes += $(SCRIMMAGE_GIT_ROOT)/include/scrimmage
  includes += $(SCRIMMAGE_ROOT)/include

  libpaths += $(SCRIMMAGE_GIT_ROOT)/build/lib
  libpaths += $(SCRIMMAGE_ROOT)/lib

  libs += scrimmage-core scrimmage-viewer scrimmage-protos scrimmage-msgs
}
