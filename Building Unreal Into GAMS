working instructions:


1. Clone gams/madara. Run it with base_build.sh prereqs unreal gams madara airlib clang
(This will pull a working Simbotic from my repo, and stock AirSim from microsoft which lives inside Simbotic/AirSim. It will then build AirSim, and then build Simbotic.

1. Clone Unreal (v4.18, v4.21 doesn't work with it, why the fuck would we use it? , build it)

2. Clone AirSim, build it.

3. Run unreal editor

4. Run AirSim project inside of unreal editor

2. Run the gams_controller.


