project {
  includes += $(OSC_ROOT)

  specific(prop:microsoft) {
    Debug::libpaths += $(OSC_ROOT)/Debug
    Release::libpaths += $(OSC_ROOT)/Release
    
    lit_libs += oscpack
  } else {
    libs += oscpack
  }

}
